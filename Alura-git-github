		https://git-scm.com/docs/git#_git_commands
git init	Cria um repositório Git	https://git-scm.com/docs/git-init
git init --bare	Cria um repositório puro somente com as alterções dos arquivos 
git status	Lista os status do repositório (branch, commits, Untracked files, Changes to be committed)	https://git-scm.com/book/pt-br/v2/Fundamentos-de-Git-Gravando-Altera%C3%A7%C3%B5es-em-Seu-Reposit%C3%B3rio
git config --global user.name "Seu nome aqui"	Configura user.name para todos repositórios	https://git-scm.com/book/en/v2/Customizing-Git-Git-Configuration
git config --local user.name "Seu nome aqui"	Configura user.name no repositório
git config --global user.email "seu@email.aqui"	Configura user.email para todos repositórios
git config --local user.email "seu@email.aqui"	Configura user.email no repositório
git config -l	Lista todas configurações globais e locais do Git
git config --global -l	Lista somente as configurações globais
git config --local -l	Lista somente as configurações locais
git add <file>	Adiciona o conteúdo do arquivo ao índice	https://git-scm.com/docs/git-add
git add .	Adiciona os conteúdos dos arquivos ao índice
git rm --cached <file>	Remove o arquivo da árvore de trabalho e do índice	https://git-scm.com/docs/git-rm
git rm --cached	Remove todos arquivos da árvore de trabalho e do índice
git commit "mensagem"	Monitora todos arquivos monitorados
git log	Lista os commits realizados	https://devhints.io/git-log
git log --oneline	Lista os commits realizados resumidamente
git log -p	Lista os commits realizados detalhadamente
git log --pretty="format:%H"	Lista os commits conforme formatação	https://devhints.io/git-log-format
git remote	Lista todos repositórios remotos configurados	https://git-scm.com/docs/git-remote
git remote -v	Lista todos repositórios remotos com url depois do nome
git remote add local C:\wagner.galvao\node\git-repository Cria um repositório remoto na pasta indicada
git remote rename origin local	Renomeia o repositório remoto
git clone C:\wagner.galvao\node\git-repository git-github2 Clona um repositório em um novo diretório (git-github2)	https://git-scm.com/docs/git-clone
git push local master	Atualizar o repositório remoto (local) com a árvore de trabalho e indice da branch (master) do repositório	https://git-scm.com/docs/git-push
git push local v0.1.0	Atualizar o repositorio remoto (local) com a tag (v0.1.0)
git pull local master	Atualizar a árvore de trabalho e índice da branch (master) do repositório com a árvore de trabalho e índice do repositorio remoto (local)	https://git-scm.com/docs/git-pull
git branch	lista, cria ou exclui ramos	https://git-scm.com/docs/git-branch
git branch titulo	cria o ramo (titulo)
git checkout titulo	Alterna para o ramo (titulo)	https://git-scm.com/docs/git-checkout
git checkout -b lista	Cria e alterna para o ramo (lista)
git checkout --index.html	Alterna (index.html) para a versão do último commit
git checkout bd995a8	
git merge titulo	Junta o ramo (titulo) no ramo atual	https://git-scm.com/docs/git-merge
git rebase lista	Replica os commits do rmano (lista) no ramo atual	https://git-scm.com/docs/git-rebase
git rebase X git merge		https://medium.com/datadriveninvestor/git-rebase-vs-merge-cc5199edd77c
git reset HEAD index.html	Redefine o HEAD atual do arquivo (index.html) para o status informado.
git revert bd995a8	Reverte o commit do hash indicado	https://git-scm.com/docs/git-revert
git stash	Salva separadamente as alterações temporárias	https://git-scm.com/docs/git-stash
git stash list Lista as alterações temporárias
git stash pop	Recupera e aplica as últimas alterações temporárias e remove da lista
git diff	Exibe as alterações que ainda não foram adicionadas ao índice	https://git-scm.com/docs/git-diff
git diff bd995a8..70eef30 Exibe as alterações do commit (bd995a8) até (70eef30)
git tag -a v0.1.0 -m "mensagem"	Criar um objeto de tag (v0.1.0) com a mensagem (mensagem)	https://git-scm.com/docs/git-tag
git tag	Lista todas as tags

